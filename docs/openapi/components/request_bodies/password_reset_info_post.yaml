description: |
  Start the process reset process.
content:
  application/json:
    schema:
      $ref: '../schemas/password_reset_info.yaml'
    example:
      $ref: '../examples/password_reset_info.yaml'
required: true
