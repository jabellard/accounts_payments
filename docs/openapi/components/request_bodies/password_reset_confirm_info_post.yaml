description: |
  Finalize the password reset process.
content:
  application/json:
    schema:
      $ref: '../schemas/password_reset_confirm_info.yaml'
    example:
      $ref: '../examples/password_reset_confirm_info.yaml'
required: true
