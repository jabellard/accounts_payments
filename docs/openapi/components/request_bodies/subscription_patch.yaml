description: |
  Subscription update request body.
content:
  application/json:
    schema:
      $ref: '../schemas/subscription_patch.yaml'
    example:
      $ref: '../examples/subscription_patch.yaml'
required: true
