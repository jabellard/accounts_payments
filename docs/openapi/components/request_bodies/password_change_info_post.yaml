description: |
  New password info.
content:
  application/json:
    schema:
      $ref: '../schemas/password_change_info.yaml'
    example:
      $ref: '../examples/password_change_info.yaml'
required: true
